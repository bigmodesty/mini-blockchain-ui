# mini-blockchain-ui/ 
                          <-- root of repo
├── index.html            <-- full HTML + JS + CSS inline
├── script.js             <-- optional separate JS (for clarity)
├── style.css             <-- optional separate CSS
└── vercel.json           <-- tells Vercel it’s a static site
